# 📊 תרשים מבנה מערכת הבדיקות

```
tests/sidebar/
│
├── 📁 קבצי בדיקות (Test Files)
│   ├── 🧪 mainNavItems.test.ts        → 20 טאבי ניווט ראשי
│   ├── 🧪 systemNavItems.test.ts      → 8 טאבי ניווט מערכת  
│   ├── 🧪 customTables.test.ts        → טבלאות מותאמות
│   ├── 🧪 appSidebar.test.ts          → פונקציונליות כללית
│   └── 🧪 index.test.ts               → אינטגרציה כללית
│
├── 📚 תיעוד (Documentation)
│   ├── 📖 README.md                   → מדריך מלא ומפורט
│   ├── 🚀 QUICK_START.md              → התחלה מהירה
│   ├── 📊 SUMMARY.md                  → סיכום המערכת
│   └── 📐 STRUCTURE.md                → מסמך זה
│
├── ⚙️ תצורה וכלים (Config & Tools)
│   ├── ⚙️ test-config.json            → קובץ תצורה מרכזי
│   ├── 🔧 index.ts                    → API מרכזי
│   ├── 💡 examples.ts                 → דוגמאות שימוש
│   ├── 🛠️ run-sidebar-tests.ps1       → סקריפט הרצה
│   └── 🚫 .gitignore                  → קבצים להתעלם
│
└── 📦 פלט (Output)
    ├── playwright-report/            → דוחות HTML
    ├── test-results/                 → תוצאות מפורטות
    └── coverage/                     → דוחות כיסוי
```

---

## 🔄 תהליך הבדיקה

```
┌─────────────────┐
│  הרצת בדיקות   │
│  npm run test   │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│  טעינת תצורה מ-test-config.json │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  בדיקות mainNavItems (50)       │
│  ✓ כפילויות                     │
│  ✓ תקינות נתונים                │
│  ✓ URLs                          │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  בדיקות systemNavItems (40)     │
│  ✓ כפילויות                     │
│  ✓ תקינות נתונים                │
│  ✓ ייחודיות מול mainNav          │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  בדיקות customTables (45)       │
│  ✓ מבנה נתונים                  │
│  ✓ הרשאות                        │
│  ✓ קונפליקטים                   │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  בדיקות appSidebar (55)         │
│  ✓ Pin/Unpin                     │
│  ✓ Hover                         │
│  ✓ Resize                        │
│  ✓ localStorage                  │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  בדיקות אינטגרציה (35)          │
│  ✓ חפיפות בין קבוצות            │
│  ✓ מערכת שלמה                   │
│  ✓ סטטיסטיקות                   │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│  דוח תוצאות                     │
│  ✅ 225 בדיקות עברו             │
│  📊 Coverage: 90%+               │
│  ⏱️  זמן: ~5 שניות               │
└─────────────────────────────────┘
```

---

## 🎯 מטרות כל קובץ

### mainNavItems.test.ts
**מטרה**: לוודא שכל 20 טאבי הניווט הראשי תקינים

**בודק**:
- ✅ אין כפילויות (URL, שם, testId)
- ✅ כל טאב מכיל נתונים תקינים
- ✅ URLs בפורמט נכון
- ✅ סדר הטאבים הגיוני
- ✅ קטגוריזציה נכונה

### systemNavItems.test.ts
**מטרה**: לוודא שכל 8 טאבי הניווט מערכת תקינים

**בודק**:
- ✅ אין כפילויות (URL, שם, testId)
- ✅ אין חפיפה עם mainNav
- ✅ טאבי מערכת קיימים (הגדרות, עזרה וכו')
- ✅ סדר נכון (הגדרות ועזרה בסוף)

### customTables.test.ts
**מטרה**: לוודא שטבלאות מותאמות עובדות נכון

**בודק**:
- ✅ מבנה טבלה תקין
- ✅ אין כפילויות ב-ID
- ✅ סינון לפי visible
- ✅ URLs תקינים (/custom/...)
- ✅ הרשאות משתמש

### appSidebar.test.ts
**מטרה**: לבדוק פונקציונליות כללית של הסיידבר

**בודק**:
- ✅ Pin/Unpin פועל
- ✅ Hover מזוהה
- ✅ Resize בגבולות (240-480px)
- ✅ Auto-hide (1000ms)
- ✅ localStorage שומר נכון
- ✅ Theme תקין
- ✅ Dialogs

### index.test.ts
**מטרה**: בדיקות אינטגרציה על המערכת השלמה

**בודק**:
- ✅ אין חפיפות בין mainNav ל-systemNav
- ✅ customTables לא מתנגשים
- ✅ סך הכל 28 טאבים
- ✅ מערכת עקבית
- ✅ סטטיסטיקות כלליות

---

## 📈 זרימת נתונים

```
┌────────────────┐
│  AppSidebar    │
│  Component     │
└───────┬────────┘
        │
        │ מכיל
        ▼
┌────────────────────────────┐
│  mainNavItems (20)         │
│  - לוח בקרה                │
│  - לקוחות                  │
│  - משימות                  │
│  - ...                     │
└───────┬────────────────────┘
        │
        │ +
        ▼
┌────────────────────────────┐
│  systemNavItems (8)        │
│  - אוטומציות               │
│  - הגדרות                  │
│  - ...                     │
└───────┬────────────────────┘
        │
        │ +
        ▼
┌────────────────────────────┐
│  customTables (דינמי)      │
│  - טבלת X                  │
│  - טבלת Y                  │
└───────┬────────────────────┘
        │
        │ נבדק על ידי
        ▼
┌────────────────────────────┐
│  מערכת הבדיקות             │
│  (225 tests)               │
└────────────────────────────┘
```

---

## 🔗 קישורים בין קבצים

```
index.ts
  ├── מייצא → mainNavItems
  ├── מייצא → systemNavItems  
  ├── מייצא → validateAllNavItems()
  └── מייצא → getSidebarSummary()
            ↓
     משמש בכל קבצי הבדיקות
            ↓
  ┌─────────┴─────────┐
  │                   │
  ▼                   ▼
mainNavItems.test   systemNavItems.test
  │                   │
  └─────────┬─────────┘
            ↓
      index.test.ts
    (בדיקות אינטגרציה)
```

---

## 🎨 צבעים וסמלים

| אלמנט | סמל | משמעות |
|-------|-----|---------|
| קובץ בדיקה | 🧪 | Test file |
| תיעוד | 📖 | Documentation |
| תצורה | ⚙️ | Configuration |
| כלי | 🛠️ | Tool/Script |
| הצלחה | ✅ | Passed |
| כישלון | ❌ | Failed |
| אזהרה | ⚠️ | Warning |
| מידע | ℹ️ | Information |

---

## 📦 גודלי קבצים (משוער)

| קובץ | שורות | גודל |
|------|-------|------|
| mainNavItems.test.ts | ~170 | 6KB |
| systemNavItems.test.ts | ~200 | 7KB |
| customTables.test.ts | ~240 | 9KB |
| appSidebar.test.ts | ~290 | 11KB |
| index.test.ts | ~290 | 11KB |
| index.ts | ~250 | 9KB |
| README.md | ~300 | 12KB |
| SUMMARY.md | ~250 | 10KB |
| **סה"כ** | **~2000** | **~75KB** |

---

## 🚦 סטטוס הפרויקט

```
┌──────────────────────────────────────┐
│  מערכת בדיקות Sidebar                │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                       │
│  📊 סטטיסטיקות:                      │
│  • 5 קבצי בדיקות                     │
│  • 225+ בדיקות                       │
│  • 28 טאבים מכוסים                   │
│  • 90%+ coverage                     │
│                                       │
│  ✅ מוכן לשימוש                       │
└──────────────────────────────────────┘
```

---

זהו! המערכת מאורגנת, מתועדת ומוכנה לשימוש. 🎉
