# מערכת בדיקות לסיידבר - Sidebar Tests

מערכת בדיקות מקיפה עבור כל הטאבים והפונקציונליות של הסיידבר.

## 📁 מבנה הקבצים

```
tests/sidebar/
├── README.md                    # מסמך זה
├── index.test.ts               # בדיקות כלליות ואינטגרציה
├── mainNavItems.test.ts        # בדיקות לטאבי ניווט ראשי
├── systemNavItems.test.ts      # בדיקות לטאבי ניווט מערכת
├── customTables.test.ts        # בדיקות לטבלאות מותאמות
└── appSidebar.test.ts          # בדיקות לפונקציונליות כללית
```

## 🎯 מטרות הבדיקות

### 1. בדיקת כפילויות
- **אין כפילויות ב-URL** - כל טאב מוביל לכתובת ייחודית
- **אין כפילויות בשמות** - כל טאב מכיל שם ייחודי
- **אין כפילויות ב-ID** - כל טאב מזוהה באופן ייחודי

### 2. בדיקת תקינות נתונים
- כל טאב מכיל את כל השדות הנדרשים
- שמות וכתובות תקינים
- Icons תקינים וזמינים
- URLs בפורמט נכון

### 3. בדיקת פונקציונליות
- Pin/Unpin עובד כראוי
- Hover מזוהה נכון
- Resize פועל בגבולות נכונים
- Auto-hide מתנהג כמצופה
- שמירה ב-localStorage

## 📊 סטטיסטיקות

### טאבי ניווט ראשי (mainNavItems)
- **סך הכל**: 20 טאבים
- **קטגוריות**:
  - לוח בקרה ודשבורד
  - ניהול לקוחות ועובדים
  - ניהול זמן
  - משימות ופגישות
  - דוחות וכספים
  - תקשורת (Gmail, שיחות)
  - קבצים ומסמכים

### טאבי ניווט מערכת (systemNavItems)
- **סך הכל**: 8 טאבים
- **קטגוריות**:
  - אוטומציות ואנליטיקס
  - מעקב והיסטוריה
  - גיבויים וניהול
  - הגדרות ועזרה

### טבלאות מותאמות (customTables)
- **דינמי** - מספר משתנה לפי המשתמש
- **בדיקות**:
  - תקינות נתונים
  - הרשאות משתמש
  - יצירה ומחיקה
  - URLs תקינים

## 🔍 סוגי הבדיקות

### 1. Unit Tests
בדיקות לפונקציות בודדות:
- `togglePin()` - החלפת מצב pin
- `handleMouseEnter()` - זיהוי כניסה לסיידבר
- `handleMouseLeave()` - זיהוי יציאה מהסיידבר

### 2. Integration Tests
בדיקות לאינטראקציה בין רכיבים:
- Pin + Auto-hide
- Hover + Open
- Resize + Save to localStorage

### 3. Data Validation Tests
בדיקות לתקינות נתונים:
- בדיקת כפילויות
- בדיקת פורמט URLs
- בדיקת קיום שדות נדרשים

## 🚀 הרצת הבדיקות

### הרצת כל הבדיקות
```bash
npm test tests/sidebar
```

### הרצת בדיקה ספציפית
```bash
# בדיקות ניווט ראשי
npm test tests/sidebar/mainNavItems.test.ts

# בדיקות ניווט מערכת
npm test tests/sidebar/systemNavItems.test.ts

# בדיקות טבלאות מותאמות
npm test tests/sidebar/customTables.test.ts

# בדיקות פונקציונליות כלליות
npm test tests/sidebar/appSidebar.test.ts
```

### הרצה עם coverage
```bash
npm test -- --coverage tests/sidebar
```

## ✅ מה נבדק בכל קובץ

### mainNavItems.test.ts
- ✓ 20 טאבים ייחודיים
- ✓ אין כפילויות ב-URLs
- ✓ אין כפילויות בשמות
- ✓ כל טאב מכיל icon
- ✓ URLs בפורמט kebab-case
- ✓ סדר טאבים הגיוני
- ✓ קטגוריזציה נכונה

### systemNavItems.test.ts
- ✓ 8 טאבים ייחודיים
- ✓ אין כפילויות ב-URLs
- ✓ אין חפיפה עם mainNav
- ✓ הגדרות ועזרה בסוף
- ✓ טאבי ניהול מערכת קיימים
- ✓ URLs תקינים

### customTables.test.ts
- ✓ מבנה טבלה תקין
- ✓ אין כפילויות ב-ID
- ✓ סינון לפי visible
- ✓ שמות תקינים
- ✓ Icons תקינים
- ✓ URLs נבנים נכון
- ✓ מיון תקין
- ✓ הרשאות תקינות
- ✓ טיפול בשגיאות

### appSidebar.test.ts
- ✓ Pin/Unpin פועל
- ✓ Hover מזוהה
- ✓ Resize בגבולות
- ✓ Auto-hide עובד
- ✓ localStorage שומר נכון
- ✓ Gestures config תקין
- ✓ Theme תקין
- ✓ Dialogs מתנהגים נכון
- ✓ Widget edit mode
- ✓ Edge trigger

## 🐛 דוגמאות לשגיאות שנמצאות

### כפילות URL
```typescript
// ❌ שגיאה
{ title: 'לקוחות', url: '/clients' }
{ title: 'רשימת לקוחות', url: '/clients' }  // כפילות!

// ✅ תקין
{ title: 'לקוחות', url: '/clients' }
{ title: 'טבלת לקוחות', url: '/datatable-pro' }
```

### URL לא תקין
```typescript
// ❌ שגיאה
{ title: 'לקוחות', url: 'clients' }  // חסר /
{ title: 'לקוחות', url: '/Clients' }  // אותיות גדולות
{ title: 'לקוחות', url: '/my clients' }  // יש רווח

// ✅ תקין
{ title: 'לקוחות', url: '/clients' }
{ title: 'לקוחות שלי', url: '/my-clients' }
```

### חוסר שדה נדרש
```typescript
// ❌ שגיאה
{ title: 'לקוחות', url: '/clients' }  // חסר icon

// ✅ תקין
{ title: 'לקוחות', url: '/clients', icon: Users }
```

## 📈 מדדי איכות

### Coverage מצופה
- **Lines**: > 90%
- **Branches**: > 85%
- **Functions**: > 90%
- **Statements**: > 90%

### זמני ריצה
- כל קובץ: < 2 שניות
- סך הכל: < 10 שניות

## 🔄 עדכון הבדיקות

כאשר מוסיפים טאב חדש:

1. הוסף את הטאב ל-AppSidebar.tsx
2. הוסף את הטאב לבדיקות המתאימות:
   - mainNavItems.test.ts (אם זה טאב ראשי)
   - systemNavItems.test.ts (אם זה טאב מערכת)
3. עדכן את מספר הטאבים המצופה
4. הרץ את הבדיקות

דוגמה:
```typescript
// mainNavItems.test.ts
it('מספר הטאבים הוא כפי שמצופה', () => {
  expect(mainNavItems.length).toBe(21);  // עדכן מ-20 ל-21
});
```

## 🛠️ כלים נוספים

### Debugging
```bash
# הרצה במצב debug
npm test -- --debug tests/sidebar

# הצגת פלט מפורט
npm test -- --verbose tests/sidebar
```

### Watch mode
```bash
# הרצה אוטומטית בכל שינוי
npm test -- --watch tests/sidebar
```

## 📚 תיעוד נוסף

- [Playwright Documentation](https://playwright.dev/)
- [Testing Best Practices](https://playwright.dev/docs/best-practices)
- [Test Reporter](https://playwright.dev/docs/test-reporters)

## 🤝 תרומה

כדי להוסיף בדיקות חדשות:

1. צור קובץ test חדש בתיקייה
2. עקוב אחר המבנה הקיים
3. הוסף תיעוד מפורט
4. הרץ את כל הבדיקות
5. עדכן את README זה

## 📞 צור קשר

לשאלות או בעיות, פנה למפתח הראשי.
