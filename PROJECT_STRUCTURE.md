# 📁 מבנה הפרויקט - מדריך מהיר

## 📂 התיקיות הראשיות

### `src/` - קוד המקור
```
src/
├── components/        ← כל הקומפוננטות של React
│   ├── clients/       ← ניהול לקוחות
│   ├── dashboard/     ← דשבורד
│   ├── email/         ← מערכת מיילים
│   ├── layout/        ← תבנית האתר
│   └── ...
├── hooks/             ← React Hooks מותאמים
├── lib/               ← פונקציות עזר
├── pages/             ← דפי האפליקציה
├── services/          ← שירותים (API calls)
└── integrations/      ← אינטגרציות (Supabase)
```

### `supabase/` - מסד נתונים
```
supabase/
├── migrations/        ← שינויים במסד הנתונים
├── functions/         ← Edge Functions
└── config.toml        ← הגדרות Supabase
```

### `public/` - קבצים סטטיים
```
public/
├── images/            ← תמונות
├── fonts/             ← גופנים
└── icons/             ← אייקונים
```

### `scripts/` - סקריפטים עזר
```
scripts/
├── check-*.mjs        ← בדיקות
├── import-*.mjs       ← ייבוא נתונים
└── run-*.mjs          ← הרצת פעולות
```

## 🔑 קבצים חשובים

| קובץ | תיאור |
|------|-------|
| `.env` | משתני סביבה (מפתחות Supabase) |
| `package.json` | תלויות ופקודות npm |
| `vite.config.ts` | הגדרות Vite |
| `tsconfig.json` | הגדרות TypeScript |
| `tailwind.config.ts` | הגדרות Tailwind CSS |

## 📝 קבצי תיעוד

- `START_HERE.md` - התחלה מהירה
- `LOGIN_INFO.md` - פרטי כניסה
- `README.md` - מידע על הפרויקט
- `SYNC_SETUP.md` - הגדרת סינכרון

## 🚀 לפתוח תיקיות ב-VS Code

### Sidebar Explorer:
1. לחץ על האייקון הראשון בצד שמאל (📁)
2. תראה את כל התיקיות והקבצים

### פתיחת קובץ מהירה:
- `Ctrl + P` - חפש קובץ לפי שם
- `Ctrl + Shift + F` - חיפוש בכל הקבצים

### תיקיות חשובות לפתוח:
```
src/components/     ← הקוד העיקרי
src/pages/          ← הדפים
supabase/           ← מסד נתונים
```

## 🎯 קבצים שכדאי להכיר:

1. **src/App.tsx** - הקובץ הראשי
2. **src/integrations/supabase/client.ts** - חיבור ל-Supabase
3. **src/components/layout/AppHeader.tsx** - header
4. **package.json** - כל הפקודות

---

**עכשיו תראה את כל הקבצים ב-Sidebar של VS Code!** 📂
