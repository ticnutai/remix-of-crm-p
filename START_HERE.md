# 🚀 מדריך התחלה מהירה - CRM Pro

## פקודות חשובות

### להפעלת המערכת:
```bash
npm run dev
# או
npm start
```

### לכניסה למערכת:
```bash
npm run login
```
פותח את הדפדפן ומציג את פרטי הכניסה

### לבדיקת מצב המערכת:
```bash
npm run test-system
```

### לסנכרון עם GitHub:
```bash
npm run sync
```

---

## 🔐 פרטי כניסה

**אימייל**: `jj1212t@gmail.com`  
**סיסמה**: `543211`

---

## ❓ פתרון בעיות

### השרת לא עובד?
```bash
# בדוק אם יש תהליכי Node רצים
Get-Process | Where-Object {$_.ProcessName -like "*node*"}

# עצור תהליכים ישנים
Stop-Process -Name node -Force

# התחל מחדש
npm run dev
```

### לא מצליח להיכנס?
1. ודא שהשרת רץ (צריך לראות: http://localhost:8080)
2. פתח F12 בדפדפן ובדוק שגיאות ב-Console
3. נסה לרענן את הדף (Ctrl+R)
4. נקה cache (Ctrl+Shift+Delete)

### הדפדפן לא נפתח?
```bash
# פתח ידנית:
start http://localhost:8080
```

---

## 📁 קבצים חשובים

- `quick-login.ps1` - עוזר כניסה מהיר
- `test-system.ps1` - בדיקת מערכת
- `start-dev-server.ps1` - הפעלת שרת פיתוח
- `.env` - הגדרות סביבה (כולל מפתחות Supabase)
- `LOGIN_INFO.md` - מידע על הכניסה

---

## 🔗 קישורים

- **אפליקציה**: http://localhost:8080
- **Supabase Dashboard**: https://supabase.com/dashboard/project/eadeymehidcndudeycnf
- **GitHub**: https://github.com/ticnutai/remix-of-crm-p

---

**עזרה נוספת?** הרץ `npm run login` ועקוב אחר ההוראות על המסך!
