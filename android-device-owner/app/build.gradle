plugins {
    id "com.android.application"
}

android {
    namespace "com.ncrm.deviceowner"
    compileSdk 35

    buildFeatures {
        buildConfig true
    }

    defaultConfig {
        applicationId "com.ncrm.deviceowner"
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName "1.0.0"

        buildConfigField "String", "DEFAULT_CRM_URL", "\"https://example.com\""
    }
}
